# Getting Started

This scaffold gives you a single root-level npm project for:

1. A VitePress docs site
2. A minimal kiosk-facing page served as static output

## Commands

- `npm run docs:dev` — run docs locally
- `npm run docs:build` — build static docs/site output
- `npm run docs:preview` — preview built output
